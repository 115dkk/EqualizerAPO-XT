# Logos

`app-icon.png`는 Editor 실행 파일의 아이콘(`Editor/icons/app-icon.ico`, `Editor.rc`의 `IDI_ICON1`)에서 256px 프레임을 꺼낸 것이다. 설치기와 작업 표시줄, 창 캡션이 같은 아이콘을 쓴다. 워드마크는 없다. 이름은 활자로 쓴다. 제품명은 `EqualizerAPO-XT`, 설치기 표시 이름은 `EQ APO XT`이고, rack의 각인은 `EAPO-XT SERIES`다.
